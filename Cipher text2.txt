""""                       Task - 02 (Implement Caesar Cipher)
Create a Python program that can encrypt and decrypt text using the caesar Cipher algorithm.
Allow users to input a message and a shift value to perform encryption and decryption.  """

letters = 'abcdefghijklmnopqrstuvwxyz'
num_letters = len(letters)

def encrypt(plaintext, key):
    ciphertext = ''
    for letter in plaintext:
        letter = letter.lower()
        if not letter == ' ':
            index = letters.find(letter)
            if index == -1:
                ciphertext += letter
            else:
                new_index = index + key
                if new_index >= num_letters:
                    new_index -= num_letters
                ciphertext += letters[new_index]
    return ciphertext

def decrypt(ciphertext, key):
    plaintext = ''
    for letter in ciphertext:
        letter = letter.lower()
        if not letter == ' ':
            index = letters.find(letter)
            if index == -1:
                plaintext += letter
            else:
                new_index = index - key
                if new_index < 0:
                    new_index += num_letters
                plaintext += letters[new_index]
    return plaintext

print()
print('*** CAESAR CIPHER PROGRAM ***')
print()

print('Do you want to encrypt or decrypt?')
user_input = input('e/d: ').lower()
print()

if user_input == 'e':
    print('ENCRYPTION MODE SELECTED:')
    print()
    key = int(input('Enter the key( 1 through 26): '))
    text = input('Enter the text to encrypt: ')
    ciphertext = encrypt(text, key)
    print(f'CIPHERTEXT: {ciphertext}')

elif user_input == 'd':
    print('DECRYPTION MODE SELECTED:')
    print()
    key = int(input('Enter the key (1 through 26): '))
    text = input('Enter the text to decrypt: ')
    plaintext = decrypt(text, key)
    print(f'PLAINTEXT: {plaintext}')



Output Caesar Cipher--------------

*** CAESAR CIPHER PROGRAM ***     

Do you want to encrypt or decrypt?
Warning: PowerShell detected that you might be using a screen reader and has disabled PSReadLine for compatibility purposes. If you want to re-enable it, run 'Import-Module PSReadLine'.

PS E:\Program> & C:/Users/hp/AppData/Local/Programs/Python/Python311/python.exe e:/Program/Cipher.py

*** CAESAR CIPHER PROGRAM ***

Do you want to encrypt or decrypt?
e/d: e

ENCRYPTION MODE SELECTED:

enable it, run 'Import-Module PSReadLine'.

PS E:\Program> & C:/Users/hp/AppData/Local/Programs/Python/Python311/python.exe e:/Program/Cipher.py

*** CAESAR CIPHER PROGRAM ***

Do you want to encrypt or decrypt?

*** CAESAR CIPHER PROGRAM ***

Do you want to encrypt or decrypt?
*** CAESAR CIPHER PROGRAM ***

Do you want to encrypt or decrypt?

Do you want to encrypt or decrypt?
Do you want to encrypt or decrypt?
e/d: e
e/d: e


ENCRYPTION MODE SELECTED:

Enter the key( 1 through 26): 20
Enter the text to encrypt: Hi every on I have to gain my skills in cyber security
Enter the key( 1 through 26): 20
Enter the text to encrypt: Hi every on I have to gain my skills in cyber security
CIPHERTEXT: bcypylsihcbupyniauchgsmecffmchwsvylmywolcns
Enter the text to encrypt: Hi every on I have to gain my skills in cyber security
CIPHERTEXT: bcypylsihcbupyniauchgsmecffmchwsvylmywolcns
CIPHERTEXT: bcypylsihcbupyniauchgsmecffmchwsvylmywolcns
PS E:\Program> & C:/Users/hp/AppData/Local/Programs/Python/Python311/python.exe e:/Program/Cipher.py
PS E:\Program> & C:/Users/hp/AppData/Local/Programs/Python/Python311/python.exe e:/Program/Cipher.py


*** CAESAR CIPHER PROGRAM ***
*** CAESAR CIPHER PROGRAM ***

Do you want to encrypt or decrypt?
e/d: d

DECRYPTION MODE SELECTED:

Enter the key (1 through 26): 20
Enter the text to decrypt:  bcypylsihcbupyniauchgsmecffmchwsvylmywolcns
PLAINTEXT: hieveryonihavetogainmyskillsincybersecurity
PS E:\Program>





















